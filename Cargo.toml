[package]
name = "esp32c-rt"
version = "0.1.0"
repository = "https://github.com/ducktec/esp32c-rt"
authors = ["Robert Wiewel <dev@ducktec.de>"]
edition = "2018"
description = "Minimal runtime / startup for ESP32-CX CPU's"
keywords = ["riscv", "runtime", "startup", "esp32-c3"]
license = "MIT OR Apache-2.0"
categories = [
    "embedded",
    "hardware-support",
    "no-std",
]

[dependencies]
riscv-rt = "0.8.0"
riscv = "0.7.0"


[features]
inline-asm = ["riscv/inline-asm"]

[build-dependencies]
riscv-target = "0.1.2"